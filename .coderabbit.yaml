language: en
reviews:
  profile: assertive
  request_changes_workflow: false
  high_level_summary: true
  poem: false
  auto_review:
    enabled: true
    drafts: false
    base_branches:
      - main
  path_instructions:
    - path: "AGENTS.md"
      instructions: "Check that agent rules preserve truthful career positioning and do not enable fake claims."
    - path: "modes/**"
      instructions: "Review workflow prompts for privacy, truthfulness, and non-spam behavior."
    - path: "scripts/**"
      instructions: "Check Node.js scripts for clear errors, safe file writes, and no secret handling."
chat:
  auto_reply: true
