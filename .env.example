# Occupation-Ops environment example
#
# The MVP does not require API keys.
# Keep real secrets in .env only. Do not commit .env.

# Future optional AI provider integrations can use variables like:
# OPENAI_API_KEY=replace_me
# ANTHROPIC_API_KEY=replace_me
